public class threesum {
    //brute force
    //3 loop lagayege i=0;i ->n-1 tak same j aur k ke saath and then a[i]+a[j]+a[k]==0 //time complexity o(n^3 logn
    // better solution
    // for i=0->n-1,j=i+1->n-1
}
